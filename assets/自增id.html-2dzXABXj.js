import{_ as i}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as e,o as d,e as n}from"./app-Byep4MDf.js";const l={},a=n('<p>自增主键好处：可以让主键索引尽量的保持递增顺序插入，避免了页分裂，让索引更加紧凑。</p><h2 id="一、自增主键的-空洞" tabindex="-1"><a class="header-anchor" href="#一、自增主键的-空洞"><span>一、自增主键的“空洞”</span></a></h2><h3 id="_1-自增主键存储位置" tabindex="-1"><a class="header-anchor" href="#_1-自增主键存储位置"><span>1. 自增主键存储位置</span></a></h3><p>表的结构定义存放在后缀名为 <code>.frm</code> 的文件中，但是并不会保存自增值。不同的存储引擎对于自增值的保存策略不同。</p><ul><li><p>MyISAM 引起的自增值保存在数据文件中</p></li><li><p>InnoDB 引擎的自增值，不同的版本有不同的处理方法。</p><ul><li><p>在 MySQL 5.7 之前的版本（包含 5.7），自增值保持在内存中，并没有持久化。MySQL 在重启后，第一次打开表时，会先去找自增值的最大值，然后将这个最大值加 1 后作为这个表当前的自增值。</p><p>注意：如果一个表当前数据行中最大的 id 是 10，AUTO_INCREMENT=11。这时候我们删除 <code>id=10</code> 这一行，AUTO_INCREMENT 还是 11，但如果现在重启 MySQL，重启后的 MySQL 中这张表的 AUTO_INCREMENT 就为 10。</p></li><li><p>在 MySQL 8.0 版本，将自增值的变更记录在了 redo log 中，重启的时候依靠 redo log 恢复重启之前的值。</p></li></ul></li></ul><h3 id="_2-自增值的修改策略" tabindex="-1"><a class="header-anchor" href="#_2-自增值的修改策略"><span>2. 自增值的修改策略</span></a></h3><p>在 MySQL 中，如果字段 id 被定义为 AUTO_INCREMNT，在插入一行数据的时候，自增值的行为如下：</p><ul><li>如果插入数据时 id 字段指定为 0、null 或未指定值，那么就把这个表当前的 AUTO_INCREMNT 值填到自增字段</li><li>如果插入数据时 id 字段指定了具体的值，就直接使用语句中指定的值。根据要插入的值和当前自增值的大小关系，自增值的变更结果也会有所不同。假设，要插入的值是 X，当前的自增值是 Y。 <ul><li>如果 <code>X &lt; Y</code>，那么这个表的自增值不变</li><li>如果 <code>X &gt;= Y</code>，就需要把当前自增值修改为新的自增值</li></ul></li></ul><p>新的自增值的修改算法是：从自增的初始值开始，按照自增步长为单位，持续增加。知道找到第一个大于 X 的值，即可作为新的自增值。默认情况下，自增的初始化和步长都为 1。</p><blockquote><p>在一些场景中，可能不使用自增的默认值。比如双 Master 的主备架构中，要求双写，我们就可以让一个库的自增步长设置为 2。然后两个库的自增 id 分别是偶数和奇数。避免两个库生成的主键发生冲突。</p></blockquote><p>因此有可能出现自增主键 id 不连续的情况。</p><h3 id="_3-自增值出现空洞的场景" tabindex="-1"><a class="header-anchor" href="#_3-自增值出现空洞的场景"><span>3. 自增值出现空洞的场景</span></a></h3><p>假如我们有一张表：</p><div class="language- line-numbers-mode" data-highlighter="shiki" data-ext="" data-title="" style="--shiki-light:#24292e;--shiki-dark:#abb2bf;--shiki-light-bg:#fff;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes github-light one-dark-pro vp-code"><code><span class="line"><span>CREATE TABLE `t` (</span></span>\n<span class="line"><span>  `id` int(11) NOT NULL AUTO_INCREMENT,</span></span>\n<span class="line"><span>  `c` int(11) DEFAULT NULL,</span></span>\n<span class="line"><span>  `d` int(11) DEFAULT NULL,</span></span>\n<span class="line"><span>  PRIMARY KEY (`id`),</span></span>\n<span class="line"><span>  UNIQUE KEY `c` (`c`)</span></span>\n<span class="line"><span>) ENGINE=InnoDB;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>假设表 t 中已经有 <code>(1,1,1)</code> 这条记录了，然后我再执行一条插入数据的命令：<code>insert into t values(null, 1, 1);</code></p><p>那么这条语句的执行流程为：</p><ul><li>InnoDB 发现用户没有指定自增 id 的值，则获取表 t 中当前的自增值为 2。</li><li>将要插入的行改为 <code>(2,1,1)</code>，并且将表的自增 id 值改为 3。</li><li>继续执行插入数据操作，但是由于存在 <code>c=1</code> 的记录，造成唯一键冲突。报错返回</li></ul><p>所以就产生了问题，这行语句并没有执行成功，但是却更改了自增 id。并且在此之后，再插入新的数据行时，拿到的自增 id 就是 3。也就是出现了自增主键不连续的情况。</p><p>造成的原因就是：执行语句时发生了唯一键冲突，导致自增主键不连续。同样的，事务回滚也会产生类似现象。</p><p><strong>为什么 MySQL 没有将自增值回退？</strong></p><p>假设有两个并行的事务，在申请自增 id 时，肯定要加锁，然后顺序申请。假设事务 A 申请到了 <code>id=2</code>，事务 B 申请到了 <code>id=3</code>，此时表中的自增值为 4。之后事务 B 正确提交了，但事务 A 出现唯一键冲突，如果允许事务 A 回退自增 id，也就是把表当前的自增值改为 2。就会出现这样的情况，表中已经有 <code>id=3</code> 的行，但当前的自增 id 值为 2。接下来其他事务继续执行就会申请 <code>id=2</code>，然后在申请到 <code>id=3</code> 时，就会出现主键冲突。</p><p>而如果要解决这个问题，有两种方法：</p><ul><li>每次申请自增 id 之前，先判断表中是否已经存在这个 id，如果存在，则跳过。但是这个方法的成本很高。因为这样做的话，申请自增 id 还要去主键索引树上判断 id 是否存在。</li><li>把自增 id 的锁范围扩大，必须等待一个事务执行完成并提交后，下一个事务才能申请自增 id。这个方法的成本也很高，锁的粒度太大，系统并发能力大大下降。</li></ul><p>因此，允许自增 id 回退，可能会造成 MySQL 性能的下降。所以 MySQL 在语句执行失败也不回退自增 id。</p><p>于是自增 id 只保证是递增的，而不保证是连续的。</p><h2 id="二、mysql-中不同的自增-id" tabindex="-1"><a class="header-anchor" href="#二、mysql-中不同的自增-id"><span>二、MySQL 中不同的自增 id</span></a></h2><p>自增 id 有上限，有可能被用完。自增 id 用完了会怎么样？</p><h3 id="_1-表中定义的自增-id" tabindex="-1"><a class="header-anchor" href="#_1-表中定义的自增-id"><span>1. 表中定义的自增 id</span></a></h3><div class="language- line-numbers-mode" data-highlighter="shiki" data-ext="" data-title="" style="--shiki-light:#24292e;--shiki-dark:#abb2bf;--shiki-light-bg:#fff;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes github-light one-dark-pro vp-code"><code><span class="line"><span>CREATE TABLE `t` ( </span></span>\n<span class="line"><span>	`id` int(10) unsigned NOT NULL AUTO_INCREMENT, </span></span>\n<span class="line"><span>	PRIMARY KEY (`id`)) </span></span>\n<span class="line"><span>	ENGINE=InnoDB AUTO_INCREMENT=4294967295;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>表中定义的自增 id 达到上限后的逻辑是：再申请下一个 id 时，得到的值保持不变。</p><p>当这个 id 字段是主键时，并且 id 字段的值达到了类型的上限。再进行插入的时候，由于得到的自增 id 仍为上限值，就会报主键冲突错误，由用户处理。</p><h3 id="_2-innodb-系统自增-row-id" tabindex="-1"><a class="header-anchor" href="#_2-innodb-系统自增-row-id"><span>2. InnoDB 系统自增 row_id</span></a></h3><p>如果创建的 InooDB 表没有指定主键，那么 InnoDB 会创建一个用户不可见的，长度为 6 个字节的 row_id。InnoDB 维护了一个全局的 <code>dict_sys.row_id</code> 值，所有无主键的 InnoDB 表，每插入一行数据，都将当前的 <code>dict_sys.row_id</code> 值作为要插入数据的 row_id，然后把 <code>dict_sys.row_id</code> 的值加 1。</p><p>因为 row_id 是 6 字节，范围为 <code>[0, 2^48-1]</code>。当 <code>dict_sys.row_id=2^48</code> 时，如果再有插入数据的行为来申请 row_id，会返回 0。也就是说，写入表的 row_id 是从 0 开始到 <code>2^48-1</code>，达到上限后，下一个值就是 0，然后继续循环。</p><p>造成的后果就是，如果表中已经存在 <code>row_id=N</code> 的行，新写入的行就会覆盖原有的行。使用系统自增 row_id 有可能覆盖数据，而这意味着数据丢失，影响的是数据可靠性。因此，我们最好主动创建自增主键，当超过上限时，报主键冲突，影响的可用性。而一般情况下，可靠性要优于可用性的。</p><h3 id="_3-xid" tabindex="-1"><a class="header-anchor" href="#_3-xid"><span>3. XID</span></a></h3><p>XID 是 redo log 和 binlog 中一个共同的数据字段。崩溃恢复时，会按顺序扫描 redo log：</p><ul><li>如果碰到既有 prepare、又有 commit 的 redo log，就直接提交</li><li>如果碰到只有 prepare、而没有 commit 的 redo log，就拿着 XID 去 binlog 找对应的事务。（会判断这个事务是否存在且完整，如果是则提交事务，如果不是，则回滚事务）</li></ul><p>MySQL 内部维护了一个全局变量 <code>global_query_id</code>，每次执行语句的时候将他赋值给 <code>query_id</code>，然后给这个变量加 1。如果当前语句是这个事务执行的第一条语句，那么 MySQL 还会同时把 <code>query_id</code> 赋值给这个事务的 XID。那么 XID 其实就是用来对应一个事务的。</p><p>global_query_id 是一个纯内存变量，MySQL 重启会清零。而 MySQL 重启后会生成新的 binlog 文件，这也保证了同一个 binlog 文件中，XID 一定是唯一的。</p><p>如果 global_query_id 达到上限后，就会继续从 0 开始计数。那么从理论上说，还是有可能出现同一个 binlog 中出现相同 XID 的情况。但是出现这种情况的条件很苛刻，因为 global_query_id 类型是 8 个字节，上限是 <code>2^64-1</code>。要出现这种情况，必须是如下的过程：</p><ul><li>执行一个事务，假设 XID 为 A</li><li>接下来执行 <code>2^64</code> 次 SQL 语句，让 global_query_id 回到 A</li><li>在启用一个事务，这个事务的 XID 才会为 A</li></ul><p>一般我们认为，一个事务中不会有 <code>2^64</code> 个 SQL 语句。这种可能性只会存在于理论中。</p><h4 id="_4-thread-id" tabindex="-1"><a class="header-anchor" href="#_4-thread-id"><span>4. thread_id</span></a></h4><p>线程 id 是 MySQL 中最常见的一种自增 id。系统中保存了一个全局变量 <code>thread_id_count</code>，每新建一个连接，就将 <code>thread_id_count</code> 赋值给这个新连接的线程变量。</p><p><code>thread_id_count</code> 大小为 4 字节，达到 <code>2^32-1</code> 后，会被重置为 0，然后继续循环。</p><p>但是我们不会在 <code>show processlist</code> 命令中看到两个相同的线程 id。因为在给新线程分配 thread_id 时，会检测这个 thread_id 是否被分配，如果已经分配的话，会继续自增循环，直到找到一个可用的 thread_id。</p><h3 id="_5-小结" tabindex="-1"><a class="header-anchor" href="#_5-小结"><span>5. 小结</span></a></h3><ul><li>表的自增 id 达到上限后，再申请时它的值就不会改变，进而导致继续插入数据时报主键冲突的错误。</li><li>row_id 达到上限后，则会归 0 再重新递增，如果出现相同的 row_id，后写的数据会覆盖之前的数据。</li><li>Xid 只需要不在同一个 binlog 文件中出现重复值即可。虽然理论上会出现重复值，但是概率极小，可以忽略不计。</li><li>thread_id 是我们使用中最常见的，而且也是处理得最好的一个自增 id 逻辑了。</li></ul>',49),s=[a];function o(t,p){return d(),e("div",null,s)}const h=i(l,[["render",o],["__file","自增id.html.vue"]]),_=JSON.parse('{"path":"/%E6%95%B0%E6%8D%AE%E5%BA%93/%E6%9E%81%E5%AE%A2%E6%97%B6%E9%97%B4-mysql/%E7%B4%A2%E5%BC%95/%E8%87%AA%E5%A2%9Eid.html","title":"自增id详解","lang":"zh-CN","frontmatter":{"title":"自增id详解","description":"自增主键好处：可以让主键索引尽量的保持递增顺序插入，避免了页分裂，让索引更加紧凑。 一、自增主键的“空洞” 1. 自增主键存储位置 表的结构定义存放在后缀名为 .frm 的文件中，但是并不会保存自增值。不同的存储引擎对于自增值的保存策略不同。 MyISAM 引起的自增值保存在数据文件中 InnoDB 引擎的自增值，不同的版本有不同的处理方法。 在 My...","head":[["meta",{"property":"og:url","content":"https://javabetter.cn/%E6%95%B0%E6%8D%AE%E5%BA%93/%E6%9E%81%E5%AE%A2%E6%97%B6%E9%97%B4-mysql/%E7%B4%A2%E5%BC%95/%E8%87%AA%E5%A2%9Eid.html"}],["meta",{"property":"og:site_name","content":"张意的博客"}],["meta",{"property":"og:title","content":"自增id详解"}],["meta",{"property":"og:description","content":"自增主键好处：可以让主键索引尽量的保持递增顺序插入，避免了页分裂，让索引更加紧凑。 一、自增主键的“空洞” 1. 自增主键存储位置 表的结构定义存放在后缀名为 .frm 的文件中，但是并不会保存自增值。不同的存储引擎对于自增值的保存策略不同。 MyISAM 引起的自增值保存在数据文件中 InnoDB 引擎的自增值，不同的版本有不同的处理方法。 在 My..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}],["meta",{"property":"article:author","content":"张意"}],["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"自增id详解\\",\\"image\\":[\\"\\"],\\"dateModified\\":null,\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"张意\\",\\"url\\":\\"/about-the-author/\\"}]}"]]},"headers":[{"level":2,"title":"一、自增主键的“空洞”","slug":"一、自增主键的-空洞","link":"#一、自增主键的-空洞","children":[{"level":3,"title":"1. 自增主键存储位置","slug":"_1-自增主键存储位置","link":"#_1-自增主键存储位置","children":[]},{"level":3,"title":"2. 自增值的修改策略","slug":"_2-自增值的修改策略","link":"#_2-自增值的修改策略","children":[]},{"level":3,"title":"3. 自增值出现空洞的场景","slug":"_3-自增值出现空洞的场景","link":"#_3-自增值出现空洞的场景","children":[]}]},{"level":2,"title":"二、MySQL 中不同的自增 id","slug":"二、mysql-中不同的自增-id","link":"#二、mysql-中不同的自增-id","children":[{"level":3,"title":"1. 表中定义的自增 id","slug":"_1-表中定义的自增-id","link":"#_1-表中定义的自增-id","children":[]},{"level":3,"title":"2. InnoDB 系统自增 row_id","slug":"_2-innodb-系统自增-row-id","link":"#_2-innodb-系统自增-row-id","children":[]},{"level":3,"title":"3. XID","slug":"_3-xid","link":"#_3-xid","children":[]},{"level":3,"title":"5. 小结","slug":"_5-小结","link":"#_5-小结","children":[]}]}],"git":{},"readingTime":{"minutes":8.27,"words":2482},"filePathRelative":"数据库/极客时间-mysql/索引/自增id.md","excerpt":"<p>自增主键好处：可以让主键索引尽量的保持递增顺序插入，避免了页分裂，让索引更加紧凑。</p>\\n<h2>一、自增主键的“空洞”</h2>\\n<h3>1. 自增主键存储位置</h3>\\n<p>表的结构定义存放在后缀名为 <code>.frm</code> 的文件中，但是并不会保存自增值。不同的存储引擎对于自增值的保存策略不同。</p>\\n<ul>\\n<li>\\n<p>MyISAM 引起的自增值保存在数据文件中</p>\\n</li>\\n<li>\\n<p>InnoDB 引擎的自增值，不同的版本有不同的处理方法。</p>\\n<ul>\\n<li>\\n<p>在 MySQL 5.7 之前的版本（包含 5.7），自增值保持在内存中，并没有持久化。MySQL 在重启后，第一次打开表时，会先去找自增值的最大值，然后将这个最大值加 1 后作为这个表当前的自增值。</p>\\n<p>注意：如果一个表当前数据行中最大的 id 是 10，AUTO_INCREMENT=11。这时候我们删除 <code>id=10</code> 这一行，AUTO_INCREMENT 还是 11，但如果现在重启 MySQL，重启后的 MySQL 中这张表的 AUTO_INCREMENT 就为 10。</p>\\n</li>\\n<li>\\n<p>在 MySQL 8.0 版本，将自增值的变更记录在了 redo log 中，重启的时候依靠 redo log 恢复重启之前的值。</p>\\n</li>\\n</ul>\\n</li>\\n</ul>","autoDesc":true}');export{h as comp,_ as data};
