import{_ as e}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as i,o as l,e as t}from"./app-Byep4MDf.js";const d={},s=t('<p>为了避免对源表加读锁，更稳妥的方案是将数据写到外部文本文件，然后再写回目标表。</p><h3 id="一、mysqldump-方法" tabindex="-1"><a class="header-anchor" href="#一、mysqldump-方法"><span>一、mysqldump 方法</span></a></h3><p>使用 mysqldump 命令将数据导出成一组 insert 语句。可以使用如下命令：</p><div class="language- line-numbers-mode" data-highlighter="shiki" data-ext="" data-title="" style="--shiki-light:#24292e;--shiki-dark:#abb2bf;--shiki-light-bg:#fff;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes github-light one-dark-pro vp-code"><code><span class="line"><span>mysqldump -h$host -P$port -u$user -p --add-locks=0 --no-create-info --single-transaction  --set-gtid-purged=OFF db1 t --where=&quot;a&gt;900&quot; --result-file=/client_tmp/t.sql</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>这条命令，主要参数含义如下：</p><ul><li><code>--single-transaction</code> 的作用是，在导出数据的时候不需要对表 <code>db1.t</code> 加表锁，而是使用 <code>start transaction with consistent snapshot</code> 的方法。</li><li><code>--add-locks=0</code> 设置为 0，表示在输出的文件结果中，不增加 <code>LOCK TABLES t write;</code> 语句</li><li><code>--no-create-info</code> 的意思是，不需要导出表结构</li><li><code>--set-gtid-purged=OFF</code> 表示不输出和 GTID 相关的信息</li><li><code>--result-file=/client_tmp/t.sql</code> 指定流输出文件的路径</li></ul><p>通过这条 mysqldump 命令，生成的 sql 文件中包含了符合条件下的 INSERT 语句（包含了所有数据）。</p><p>如果希望生成的文件中一条 INSERT 语句，只插入一行数据的话，可以在执行 mysqldump 命令时，加上参数 <code>--skip-extended-insert</code>。</p><p>然后我们可以将生成的 sql 文件，将数据导入到目标数据库中：</p><div class="language- line-numbers-mode" data-highlighter="shiki" data-ext="" data-title="" style="--shiki-light:#24292e;--shiki-dark:#abb2bf;--shiki-light-bg:#fff;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes github-light one-dark-pro vp-code"><code><span class="line"><span>mysql -hxxx -Pxxx -uxxx db2 -e &quot;source /client_tmp/t.sql&quot;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>其中，source 是一个客户端命令。相当于打开文件，读取一条条以分号结尾的 SQL 语句，然后将 SQL 语句发送到服务端执行。</p><h3 id="二、导出-csv-文件" tabindex="-1"><a class="header-anchor" href="#二、导出-csv-文件"><span>二、导出 CSV 文件</span></a></h3><p>直接将结果导出成 <code>.csv</code> 文件。如下将查询结果导出到服务端本地目录：</p><div class="language- line-numbers-mode" data-highlighter="shiki" data-ext="" data-title="" style="--shiki-light:#24292e;--shiki-dark:#abb2bf;--shiki-light-bg:#fff;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes github-light one-dark-pro vp-code"><code><span class="line"><span>select * from db1.t where a&gt;900 into outfile &#39;/var/lib/mysql-files/db1_t.csv&#39;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>我们需要注意以下几点：</p><ul><li><p>这条语句将结果保存在服务端机器上</p></li><li><p><code>into outfile</code> 指定了文件的生成位置，这个位置必须受参数 <code>secure_file_priv</code> 的限制。</p><p>可以使用 <code>show variables like &#39;secure_file_priv&#39;;</code> 来查看。 参数 <code>secure_file_priv</code> 的可选值和作用分别是：</p><ul><li>如果设置为 empty，表示不限制文件生成的位置，这是不安全的设置</li><li>如果设置为一个表示路径的字符串，就要求生成的文件只能放在这个指定的目录，或者他的子目录</li><li>如果设置为 NULL，就表示禁止在这个 MySQL 实例上执行 <code>select ... into outfile</code> 操作</li></ul></li><li><p>这条命令不会帮我们覆盖文件，因为需要确保 <code>/var/lib/mysql-files/db1_t.csv</code> 这个文件不存在，否则会报错退出</p></li><li><p>这条命令生成的文本文件中，原则上一个数据行对应文本文件的一行。但是，如果字段中包含换行符，在生成的文本中也会有换行符。不过类似换行符、制表符这类符号，前面都会跟上 <code>\\</code> 这个转义符，这样就可以和字段之间、数据行之间的分隔符分开。</p></li></ul><p>得到 <code>.csv</code> 导出文件后，就可以使用 <code>load data</code> 命令将数据导入到目标表中：</p><div class="language- line-numbers-mode" data-highlighter="shiki" data-ext="" data-title="" style="--shiki-light:#24292e;--shiki-dark:#abb2bf;--shiki-light-bg:#fff;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes github-light one-dark-pro vp-code"><code><span class="line"><span>load data infile &#39;/var/lib/mysql-files/db1_t.csv&#39; into table db2.t;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>这条语句的执行流程如下：</p><ul><li>打开文件 <code>/var/lib/mysql-files/db1_t.csv</code>，以制表符 <code>\\t</code> 作为字段间的分隔符，以换行符 <code>\\n</code> 作为记录之间的分隔符，进行数据读取</li><li>启动事务</li><li>判断每一行的字段数与表 <code>db2.t</code> 是否相同。若不相同，则直接报错，事务回滚；若相同，则构造成一行，调用 InnoDB 引擎接口，写入到表中</li><li>重复上一步骤，直到 <code>/var/lib/mysql-files/db1_t.csv</code> 整个文件读入完成，提交事务</li></ul><p>如果 <code>binlog_format=statement</code>，这个 load 语句记录到 binlog 中后，如何在备库重放？因为备库的本地机器上没有这个文件。</p><ul><li>主库执行完成后，将 <code>/var/lib/mysql-files/db1_t.csv</code> 文件的内容直接写到 binlog 文件中</li><li>往 binlog 文件中写入语句 <code>load data local infile &#39;/tmp/SQL_LOAD_MB-1-0&#39; into table db2.t;</code></li><li>把这个 binlog 日志传到备库</li><li>备库的 apply 线程在执行这个事务日志时，先将 binlog 中的 <code>t.csv</code> 文件的内容读出来，写入到本地临时目录 <code>/tmp/SQL_LOAD_MB-1-0</code> 中；然后再执行 <code>load data</code> 语句，往备库的 <code>db2.t</code> 表中插入和主库相同的数据</li></ul><p>这里，备库执行的 <code>load data</code> 语句中，这条命令中多了一个 <code>local</code>，表示将执行这条命令的客户端所在机器的本地文件 <code>/tmp/SQL_LOAD_MB-1-0</code> 的内容，加载到 <code>db2.t</code> 中。也就是说，<code>load data</code> 命令有两种用法：</p><ul><li>不加 <code>local</code>，是读取服务端的文件，这个文件必须在 <code>secure_file_priv</code> 指定的目录或子目录下</li><li>加上 <code>local</code>，读取的是客户端的文件，只要 mysql 客户端有访问这个文件的权限即可。这时候，MySQL 客户端会先把本地文件传给服务端，然后执行上述的 <code>load data</code> 流程。</li></ul><p>需要注意，<code>select ... into outfile</code> 方法不会生成表结构文件，所以我们导出数据时还需要单独的命令得到表结构定义。mysqldump 提供了一个 <code>-tab</code> 参数，可以同时导出表结构定义文件和 csv 数据文件。这条命令的使用方法如下：</p><div class="language- line-numbers-mode" data-highlighter="shiki" data-ext="" data-title="" style="--shiki-light:#24292e;--shiki-dark:#abb2bf;--shiki-light-bg:#fff;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes github-light one-dark-pro vp-code"><code><span class="line"><span>mysqldump -h$host -P$port -u$user --single-transaction  --set-gtid-purged=OFF db1 t --where=&quot;a&gt;900&quot; --tab=$secure_file_priv</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>这个命令会在 <code>$secure_file_priv</code> 定义的目录下，创建一个 <code>t.sql</code> 文件保存建表语句，同时创建一个 <code>t.txt</code> 文件保存 csv 文件。</p><h3 id="三、物理拷贝方法" tabindex="-1"><a class="header-anchor" href="#三、物理拷贝方法"><span>三、物理拷贝方法</span></a></h3><p>上面两种办法都是逻辑导数据的方法，也就是从表 <code>db1.t</code> 中读出来，生成文本，然后再写入目标表 <code>db2.t</code> 中。这里，我们介绍下物理导数据的方式。</p><p>一个 InnoDB 表，除了包含两个物理文件（<code>.frm</code> 文件和 <code>.ibd</code> 文件）外，还需要在数据字典中注册。</p><p>从 <code>MySQL 5.6</code> 版本开始，引入了可传输表空间（<code>transportable tablespace</code>）的方法，可以通过导出+导入表空间的方式，实现物理拷贝表的功能。假设我们现在的目标是在 <code>db1</code> 库下，复制一个跟表 t 相同的表 r。具体的执行步骤如下：</p><ul><li>执行 <code>create table r like t;</code> 创建一个相同表结构的空表</li><li>执行 <code>alter table r discard tablespace;</code> 这时候 <code>r.ibd</code> 文件会被删除</li><li>执行 <code>flush table t for export;</code> 这时候 <code>db1</code> 目录下会生成一个 <code>t.cfg</code> 文件。同时，会使 <code>db1.t</code> 整个表处于只读状态，直到执行 <code>unlock tables;</code> 命令后才释放读锁。</li><li>在 <code>db1</code> 目录下执行 <code>cp t.cfg r.cfg; cp t.ibd r.ibd;</code> 这这两个命令。（注意：Linux 中 db1 目录的位置一般在： <code>/var/lib/mysql/db1</code> 下。然后拷贝文件，注意查看文件的权限，MySQL 进程要有读写权限）</li><li>执行 <code>unlock tables;</code> 这时候 <code>t.cfg</code> 文件会被删除</li><li>执行 <code>alter table r import tablespace;</code> 将这个 <code>r.ibd</code> 文件作为表 r 的新的表空间，由于这个文件的数据内容和 <code>t.ibd</code> 是相同的，所以表 r 中就有了和表 t 相同的数据。在执行 <code>import tablespace</code> 的时候，为了让文件中的表空间 id 和数据字典中的一致，会修改 <code>r.ibd</code> 的表空间 id。而这个表空间 id 存在于每一个数据页中。因此，如果是一个很大的文件，每个数据页都需要修改，所以会看到这个 import 语句的执行是需要一些时间的。当然，相比于逻辑导入的方式，import 语句的耗时是非常短的。</li></ul><h3 id="四、三种方法的优缺点" tabindex="-1"><a class="header-anchor" href="#四、三种方法的优缺点"><span>四、三种方法的优缺点</span></a></h3><p>我们来对比一下三种方法的优缺点。</p><ul><li>物理拷贝的方式速度最快，尤其对于大表拷贝来说是速度最快的方法。如果出现误删表的情况，用备份恢复出误删之前的临时库，然后再把临时库中的表拷贝到生产库上，是恢复数据最快的方法。但是这种方法的使用也有一定的局限性： <ul><li>必须是全表拷贝，不能只拷贝部分数据</li><li>需要在服务器上拷贝数据，在用户无法登陆数据库主机的场景下无法使用</li><li>由于是通过拷贝物理文件实现的，源表和目标表都是使用 InnoDB 引擎才能使用</li></ul></li><li>用 mysqldump 生成包含 INSERT 语句文件的方法，可以在 where 参数增加过滤条件，来实现只导出部分数据。这个方式的不足之一是，不能使用 join 这种比较复杂的 where 条件写法。</li><li>用 <code>select ... into outfile</code> 的方法是最灵活的，支持所有的 SQL 写法。但是这个方法的缺点之一就是，每次只能导出一张表的数据，而且表结构也需要另外的语句单独备份。</li></ul><p>后两种都是逻辑备份方式，是可以跨引擎使用的。</p>',36),o=[s];function c(a,r){return l(),i("div",null,o)}const h=e(d,[["render",c],["__file","快速复制一张表.html.vue"]]),u=JSON.parse('{"path":"/%E6%95%B0%E6%8D%AE%E5%BA%93/%E6%9E%81%E5%AE%A2%E6%97%B6%E9%97%B4-mysql/%E5%BF%AB%E9%80%9F%E5%A4%8D%E5%88%B6%E4%B8%80%E5%BC%A0%E8%A1%A8.html","title":"快速复制一张表","lang":"zh-CN","frontmatter":{"title":"快速复制一张表","description":"为了避免对源表加读锁，更稳妥的方案是将数据写到外部文本文件，然后再写回目标表。 一、mysqldump 方法 使用 mysqldump 命令将数据导出成一组 insert 语句。可以使用如下命令： 这条命令，主要参数含义如下： --single-transaction 的作用是，在导出数据的时候不需要对表 db1.t 加表锁，而是使用 start tr...","head":[["meta",{"property":"og:url","content":"https://javabetter.cn/%E6%95%B0%E6%8D%AE%E5%BA%93/%E6%9E%81%E5%AE%A2%E6%97%B6%E9%97%B4-mysql/%E5%BF%AB%E9%80%9F%E5%A4%8D%E5%88%B6%E4%B8%80%E5%BC%A0%E8%A1%A8.html"}],["meta",{"property":"og:site_name","content":"张意的博客"}],["meta",{"property":"og:title","content":"快速复制一张表"}],["meta",{"property":"og:description","content":"为了避免对源表加读锁，更稳妥的方案是将数据写到外部文本文件，然后再写回目标表。 一、mysqldump 方法 使用 mysqldump 命令将数据导出成一组 insert 语句。可以使用如下命令： 这条命令，主要参数含义如下： --single-transaction 的作用是，在导出数据的时候不需要对表 db1.t 加表锁，而是使用 start tr..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}],["meta",{"property":"article:author","content":"张意"}],["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"快速复制一张表\\",\\"image\\":[\\"\\"],\\"dateModified\\":null,\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"张意\\",\\"url\\":\\"/about-the-author/\\"}]}"]]},"headers":[{"level":3,"title":"一、mysqldump 方法","slug":"一、mysqldump-方法","link":"#一、mysqldump-方法","children":[]},{"level":3,"title":"二、导出 CSV 文件","slug":"二、导出-csv-文件","link":"#二、导出-csv-文件","children":[]},{"level":3,"title":"三、物理拷贝方法","slug":"三、物理拷贝方法","link":"#三、物理拷贝方法","children":[]},{"level":3,"title":"四、三种方法的优缺点","slug":"四、三种方法的优缺点","link":"#四、三种方法的优缺点","children":[]}],"git":{},"readingTime":{"minutes":7.31,"words":2194},"filePathRelative":"数据库/极客时间-mysql/快速复制一张表.md","excerpt":"<p>为了避免对源表加读锁，更稳妥的方案是将数据写到外部文本文件，然后再写回目标表。</p>\\n<h3>一、mysqldump 方法</h3>\\n<p>使用 mysqldump 命令将数据导出成一组 insert 语句。可以使用如下命令：</p>\\n<div class=\\"language- line-numbers-mode\\" data-highlighter=\\"shiki\\" data-ext=\\"\\" data-title=\\"\\" style=\\"--shiki-light:#24292e;--shiki-dark:#abb2bf;--shiki-light-bg:#fff;--shiki-dark-bg:#282c34\\"><pre class=\\"shiki shiki-themes github-light one-dark-pro vp-code\\"><code><span class=\\"line\\"><span>mysqldump -h$host -P$port -u$user -p --add-locks=0 --no-create-info --single-transaction  --set-gtid-purged=OFF db1 t --where=\\"a&gt;900\\" --result-file=/client_tmp/t.sql</span></span></code></pre>\\n<div class=\\"line-numbers\\" aria-hidden=\\"true\\" style=\\"counter-reset:line-number 0\\"><div class=\\"line-number\\"></div></div></div>","autoDesc":true}');export{h as comp,u as data};
